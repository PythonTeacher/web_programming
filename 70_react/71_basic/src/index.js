import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import ContextSample2 from './ContextSample2';

// 리액트 컴포넌트를 보여주기 위해 ReactDOM.render() 함수를 사용
// render(랜더링할 컴포넌트, 어떤 DOM에 그릴지)
ReactDOM.render(<ContextSample2 />, document.getElementById('root'));
